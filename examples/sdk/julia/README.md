# Julia SDK Example

Run from `examples/`:

```bash
make sdk-julia-example
```

The demo supports runtime loading via:

- `RULIA_MANIFEST_URL` (+ optional `RULIA_VERSION`, default `0.1.0`)
- `RULIA_LIB_PATH`
